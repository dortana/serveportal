import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
  title: 'RealIdea - Profile',
};

const ProfilePage = () => {
  return <div className='p-4 rounded-lg h-full bg-white'>Profile</div>;
};

export default ProfilePage;
