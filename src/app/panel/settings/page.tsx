import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
  title: 'ServePortal - Settings',
};

const SettingsPage = () => {
  return <div className='p-4 rounded-lg h-full bg-white'>Settings</div>;
};

export default SettingsPage;
